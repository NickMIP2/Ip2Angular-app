export class SessionCard {
  constructor(public id: number,
              public name: string,
              public distanceToCenter: number,
              public x: number,
              public y: number) {
  }
}
