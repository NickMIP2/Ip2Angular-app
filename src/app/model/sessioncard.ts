export class SessionCard {
  constructor(public id: number,
              public name: string,
              public priority: number,
              public x: number,
              public y: number) {
  }
}
